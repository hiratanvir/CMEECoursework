#include <stdio.h>

int main (void)
{
  int x = 0;

  int aninteger = 1.3475;
  char _Character = 'A';

  printf("The value of x is %i\n", x);

  printf("The value of aninteger is: %i\n", aninteger);

  return 0;
}


// double quotes are for strings, must use single quotes to assign a character
// A string is essentially an array of characters
// integral types: int, char, bool, "pointer"
// floating point types: float, double
